Sports betting api, manages matches and odds.

running with docker:
docker-compose up

app starts on port 8080

swagger: http://localhost:8080/swagger-ui.html

postman collection is included (postman_collection.json), import it to test the endpoints
